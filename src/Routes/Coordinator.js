export const goToMain = (history) => {
    history.push("/")
}

export const goToStart = (history) => {
    history.push("/inicio")
}

export const goToFollow = (history) => {
    history.push("/seguir")
}

export const goToContact = (history) => {
    history.push("/contato")
}

export const goToCasesGames = (history) => {
    history.push("/desenvolvimento")
}